# Use a random available port for the Quarkus test HTTP server so tests are
# hermetic regardless of what already runs on the default test port (8081).
# RestAssured is automatically reconfigured by Quarkus when this is set to 0.
quarkus.http.test-port=0
quarkus.http.test-ssl-port=0
